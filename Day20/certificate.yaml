apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: nginx-cert
  namespace: demo

spec:
  secretName: nginx-tls

  commonName: nginx.local

  dnsNames:
    - nginx.local

  issuerRef:
    name: selfsigned
